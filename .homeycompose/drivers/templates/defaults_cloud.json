{
  "icon": "{{driverAssetsPath}}/icon.svg",
  "pair": [
    {
      "id": "cloud_pairing_form",
      "$template": "cloud_pairing_form"
    },
    {
      "id": "add_device",
      "$template": "add_device"
    }
  ],
  "platforms": [ "cloud" ],
  "connectivity": [ "cloud" ]
}
